"""
    SERVER_HOST_DEFAULT

The default server host URL for the application. The default URL is `127.0.0.1`.
"""
const SERVER_HOST_DEFAULT = "127.0.0.1"

"""
    SERVER_PORT_DEFAULT

Default port number for the server. The default port is `8000`.
"""
const SERVER_PORT_DEFAULT = 8000

"""
    SSL_ENABLED_DEFAULT

This constant defines the default setting for SSL usage in the server setup.
If it's set to `false`, it indicates that SSL is not used by default.
"""
const SSL_ENABLED_DEFAULT = false

"""
    OPEN_CLIENT_DEFAULT

Default value to open the client automatically when the server starts. 
The default value is `false`.
"""
const OPEN_CLIENT_DEFAULT = false

"""
    OPEN_DOCUMENTATION_DEFAULT

Default value to open the documentation when the server starts for Swagger documentation. 
The default value is `false`.
"""
const OPEN_DOCUMENTATION_DEFAULT = false
